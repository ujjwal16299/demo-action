{
  "name": "zv-hello",
  "root": "plugins/hello-word/",
  "tags": ["feature:"],
  "sourceRoot": "plugins/hello-word/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm install"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "plugins/hello-word/",
        "commands": ["npm version {args.version}", "npm publish"],
        "parallel": false
      }
    }
  }
}
